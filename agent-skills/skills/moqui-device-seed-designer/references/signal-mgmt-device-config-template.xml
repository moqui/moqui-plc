<?xml version="1.0" encoding="UTF-8"?>
<!--
This software is in the public domain under CC0 1.0 Universal plus a
Grant of Patent License.

To the extent possible under law, the author(s) have dedicated all
copyright and related and neighboring rights to this software to the
public domain worldwide. This software is distributed without any
warranty.

You should have received a copy of the CC0 Public Domain Dedication
along with this software (see the LICENSE.md file). If not, see
<http://creativecommons.org/publicdomain/zero/1.0/>.
-->
<entity-facade-xml type="seed">

    <!-- Compose this fragment with device-config-template.xml. -->

    <moqui.math.Parameter parameterId="${CFG_P_CODE}" deviceConfigId="${DEVICE_CONFIG_ID}" parameterDefId="${PD_CODE}" numericValue="1"/>
    <moqui.math.Parameter parameterId="${CFG_P_CATEGORY}" deviceConfigId="${DEVICE_CONFIG_ID}" parameterDefId="${PD_CATEGORY}" numericValue="0"/>
    <moqui.math.Parameter parameterId="${CFG_P_RESET_POLICY}" deviceConfigId="${DEVICE_CONFIG_ID}" parameterDefId="${PD_RESET_POLICY}" numericValue="0"/>
    <moqui.math.Parameter parameterId="${CFG_P_OUTPUT_ACTION}" deviceConfigId="${DEVICE_CONFIG_ID}" parameterDefId="${PD_OUTPUT_ACTION}" numericValue="0"/>
    <moqui.math.Parameter parameterId="${CFG_P_ACTIVATION_CONDITION}" deviceConfigId="${DEVICE_CONFIG_ID}" parameterDefId="${PD_ACTIVATION_CONDITION}" symbolicValue="N"/>
    <moqui.math.Parameter parameterId="${CFG_P_AUTO_RESET_CONDITION}" deviceConfigId="${DEVICE_CONFIG_ID}" parameterDefId="${PD_AUTO_RESET_CONDITION}" symbolicValue="N"/>

</entity-facade-xml>
