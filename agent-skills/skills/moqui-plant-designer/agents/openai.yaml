interface:
  display_name: "Moqui Plant Designer"
  short_description: "Resume model-driven industrial design sessions"
  default_prompt: "Use $moqui-plant-designer to initialize or resume the active plant-design session and coordinate its approved seed, PLC and gateway workflow."

policy:
  allow_implicit_invocation: true
