# Survey status

This session began after the HVAC model and standard code had already been
reviewed and committed. The earlier conversation was not captured through the
structured survey workflow, so no YAML survey is represented here as an
approved historical answer.

Use `../seed-data/HVACDemoData.xml` for the reviewed model, `../session.json`
for workflow state, and `../notes/conversation-history.md` for decisions and
scope. If the design is revised, collect new structured surveys with explicit
approval rather than reconstructing approvals from chat history.
