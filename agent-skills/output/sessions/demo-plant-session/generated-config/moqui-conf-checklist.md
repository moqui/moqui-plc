# MoquiConf Review Checklist

## Framework

- `retryOnError` =
- `minRetryTime` =
- `maxRetryCount` =

## Logger

- `LOG_MAX_SIZE` =
- `defaultLogLevel` =
- `LOG_SOURCE_LIST_MAX_SIZE` =
- `LOG_JSON_PAYLOAD_MAX_SIZE` =
- `JSON_PAYLOAD_MAX_SIZE` =
- `JSON_LINE_BREAK` =
- `JSON_ELEMENT_LIST_MAX_SIZE` =
- `LOG_APPENDER_BATCH_SIZE` =
- `logTopic` =
- `logAppenderTimeout` =
- `moquiCmdsTopic` =
- `liveParamsSubTopic` =
- `liveParamsSubTimeout` =
- `liveParamsPubTopic` =
- `liveParamsPubTimeout` =

## Communication Protocols

- `fieldbus` =

## MQTT

- omitted because exposure mode is not MQTT

## Signal Management

- `SIGNAL_LIST_MAX_SIZE` =

## Device Config Management

- `DEVICE_CONFIG_LIST_MAX_SIZE` =
- `defaultConfigType` =
- `deviceConfigStoragePath` =
- `ACTUATOR_GROUP_MAX_SIZE` =
- `AXIS_IN_VELOCITY_TOLERANCE` =
